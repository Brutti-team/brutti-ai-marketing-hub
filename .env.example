# Public deployment URL only. This is safe to expose in the frontend.
VITE_APPS_SCRIPT_URL=https://script.google.com/macros/s/YOUR_DEPLOYMENT_ID/exec

# Do not add WORKSPACE_KEY, OpenAI, Google or Meta credentials here.
# Store every private value in Google Apps Script > Project Settings > Script Properties.
